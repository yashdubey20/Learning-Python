import csv    
try:
    fName = "newfile.csv"
    with open(fName, 'r') as f:
        reader = csv.reader(f)
        for row in reader:
            pass 
    
except FileNotFoundError:
    print ("Could not read file:",fName)

# Practise Again Very Important
